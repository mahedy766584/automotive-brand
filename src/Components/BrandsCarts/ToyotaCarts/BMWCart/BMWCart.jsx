import { useLoaderData } from "react-router-dom";

const BMWCart = () => {

    const bmwLoaded = useLoaderData();
    console.log(bmwLoaded)

    return (
        <div>
            
        </div>
    );
};

export default BMWCart;